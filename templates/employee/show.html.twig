{% extends 'base.html.twig' %}

{% block title %}Ver Empleado{% endblock %}

{% block body %}
    <h1>Ver Empleado</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employee.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ employee.name }}</td>
            </tr>
            <tr>
                <th>Apellido</th>
                <td>{{ employee.lastname }}</td>
            </tr>
            <tr>
                <th>Identificación</th>
                <td>{{ employee.identification }}</td>
            </tr>
            <tr>
                <th>Cargo</th>
                <td>{{ employee.position }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('employee_index') }}">Regresar a la lista</a>

    <a href="{{ path('employee_edit', {'id': employee.id}) }}">Editar</a>

    {{ include('employee/_delete_form.html.twig') }}
{% endblock %}
